import 'package:dart_leetcode/src/dart_lists.dart';

void main() {
  // print(twoSums([2, 5, 5, 11, 12], 10, {}, 0));

  // Learning dart
  DartLists().lists();
  // DartMaps().maps();
  // DartLoops().loops();
  // DartHash().hash();
}
